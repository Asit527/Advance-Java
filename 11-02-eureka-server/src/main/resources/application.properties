# 1. The name and port
spring.application.name=11-02-eureka-server
server.port=8761

# 2. Tell this instance that it is the SERVER, not a client
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false